services:
  frontend:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - ./frontend:/app/app
    environment:
      - API_URL=${API_URL}
    depends_on:
      - web